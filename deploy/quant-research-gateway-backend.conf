upstream quant_research_backend {
    # This file is deliberately the only switchover point. Editing it and
    # validating/reloading Nginx preserves accepted connections while a future
    # preflight instance is promoted to the active backend.
    server quant-research:8000;
    keepalive 16;
}
