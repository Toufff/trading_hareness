# Compatibility placeholder for deployments that mount this file separately.
# The main gateway config resolves the active backend via Docker DNS per
# request, which is required because a recreated container receives a new IP.
