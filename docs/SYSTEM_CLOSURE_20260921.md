# 全链路修复与验收台账（2026-09-21 至 09-22）

依据：G:/StockPlatform/reports/reviews/2026-09-21-system-audit-and-roadmap.md。
开发基线 3b24e04；生产基线 0d06d0864611。原始证据、正式策略评分和人工批准边界不改变。

| 项目 | 工作与验收目标 | 状态 |
|---|---|---|
| A01 | 纪律预期/实际覆盖；版本变化使旧健康失效 | 已实现；2/5 持仓、0/2 推荐的真实缺口必须保持警告，不改纪律线凑满覆盖 |
| A02 | 指数单位独立校验；有证据的历史修复 | 已实现；182 条错误隔离成交额恢复；精度误报按证据关闭；最新七指数齐全 |
| A03 | 决策语义版本与构建版本分离 | 已实现；单策略代码/设置身份不再被报告改版打断，旧身份不追溯合并 |
| A04 | 独立价格跟踪与复权收益 | 已实现；真实刷新 11,838 条评价；定向补回 18 只股票及 3 个指数日线 |
| A05 | 查询层紧凑投影、按需详情、前端实测 | 紧凑投影及同轮分页实现；本地真实 JSON 从约 28 MB 降至约 338 KB；浏览器验收见发布回执 |
| A06 | 观察周期与结构化虚拟买点 | 新周期可重新冻结；新增明确命名的日线影子买点实验；不冒充原文字条件或补造历史买点 |
| A07 | 原子治理事项的可执行推进与独立复核 | 证据采集器、职责/能力路由、重试条件和24小时复核要求已落库；独立复核和人工激活边界保留 |
| A08 | 描述性诊断与预测效果证据分层 | 已实现；真实独立日期12天，不把9,223条候选充作独立日期 |
| A09 | 机器排序/人工调整分别归因 | 新推荐登记保留原顺位、人工顺位、当时理由与未选对照；不事后回填理由 |
| A10 | 消息影响、时效、更正及消费证据 | 新增/更新/退出、内容版本、引用时点及关联候选留痕；同来源更正替代旧版本，缓存读取也检查到期 |
| A11 | 业务覆盖验收矩阵，不能只验证字段一致 | 已有只读接口/CLI；不完整覆盖、过期等待、采样缺口均不会总绿 |
| A12 | 接手现状、正式发布、隔离恢复演练 | 接手文档纠正；真实备份恢复演练通过；部署身份和线上验收另附回执 |

测试分为纯函数、隔离数据库/链路、正式发布、生产读回；每层分别记录，不以某层通过代替其余层。
预测收益需自然前瞻样本；历史场景回放仅证明机制，不宣称未来盈利。

## 实测证据与边界

生产写入仅涉及：有原始来源证据的缺失/错误隔离行情、派生候选评价、治理取证包及路由诊断。
未变更推荐池、策略权重、纪律线、账户持仓；未调用券商、未下单、未发送模拟消息。

- 后端全套：3182 passed，114 skipped，883 subtests passed（发布前；跳过不算通过）。
- 隔离真实 PostgreSQL：24 passed，包含实际 SQL、证据持久化、幂等、参数实验测量和拒绝自审。
- 参数实验夹具真实执行到 observing；空样本正确为 insufficient，未激活。它不证明真实11项均已进入实验。
- 备份恢复演练：80条首批、46条更新/新增，共126条应用，逐行哈希一致；篡改分块被拒；拒绝恢复到生产库。
- 修复前后业务矩阵：`G:/StockPlatform/reports/reviews/2026-09-22-business-*.json`。
- 历史评价与治理取证：`G:/StockPlatform/reports/reviews/2026-09-22-feedback-refresh.json`。
- 行情补查原文与回执：`G:/StockPlatform/data/research/20260922-followup-price-repair/`。

## 不能冒充已解决的内容

1. **有效纪律计划仍不齐。** 大族和百合花的新止损低于原线却无放宽原因；神奇的风险距离超过质量阈值。
   正式推荐也缺对应本轮有效线。检测/报警已修，但不能擅自放宽纪律规则或编造用户授权。
2. **历史缺口不能全部补齐。** Longhu 对 600301、603400、605577 的13个历史股票-日期请求返回1017；
   剑桥科技有一条旧发现价与标准行情锚点不一致。保留未知，不把供应商缺失换成0收益。
   定向补查后显示候选中的 data_gap 从61降至8，adjustment_gap仍1。
3. **治理不是任意代码自动开发器。** 当前11项已补齐可定位的源码/原始事项证据和下一步，
   其中部分旧事项仍需独立角色分类/复核；不能由本轮施工者伪装另一身份批准自己。
   已注册参数/影子实验可执行，任意代码提案进入工程队列，不自动改生产策略。
4. **日线虚拟买点是新研究代理。** 它明确不包含未结构化的板块/盘中VWAP等原文条件；
   原假设仍独立保留。旧华天/天融信等已知结果不倒填为该实验的前瞻交易。
5. **消息关联不是完整投资因果证明。** 现在可审查版本、有效期和关联；并未因此自动调整评分，
   也不能声称每条消息都已完成“超预期/已定价”的投资研究。

上述业务警告是独立验收结果，不能被“代码测试通过”“发布成功”覆盖。

## 可复验入口

```powershell
python scripts/verify-business-coverage.py --output G:/StockPlatform/reports/reviews/business-current.json
python scripts/run-isolated-db-tests.py tests/test_business_closure_database.py tests/test_index_amount_repair.py tests/test_daily_bar_gap_backfill.py
pwsh -NoProfile -File scripts/windows/tests/test-stock-incremental-backup-drill.ps1
```

只读 API：`GET /api/v1/strategy/business-coverage`（代理路径 `/api/research/strategy/business-coverage`）。
详情 API 必须带本页的真实 `run_id`；翻页、下载报告不得悄悄跳到更新一轮。

页面验收补修：单策略历史/效果分页先按策略过滤再分页；切换策略取消旧请求并重置页码，
明确显示已加载/全部数量，不把第一页为空说成该策略没有跟踪。
多任务发布补修：正式发布在测试前及快照前检查当前生产提交属于待发提交的祖先；
缺失身份、未合并的线上工作或测试期间 HEAD 变化一律拒绝。主动回退仍使用独立 rollback 工作流。
