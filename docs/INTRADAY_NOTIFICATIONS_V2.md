# 盘中通知定位与降噪 v2（2026-09-22）

## 用户可见契约

| 类型 | 发送条件 | 第一屏 |
|---|---|---|
| 重要变化（个股/指数/板块） | 原有确定性规则确认新变化；采样与阈值不变 | 对象、区间涨跌、放缩量或缺失说明、多空状态、计划边界 |
| 持仓纪律 / 候选条件触发 | 原纪律规则确认，独立发送，不等待模型 | 已触发的具体线、确认依据、对应人工应对 |
| 条件更新 | 十分钟检查发现与当日已通知基线相比，原计划事实真的更新；模型亦判断有必要解释 | 哪个条件、前后值、对原条件的解释 |
| 阶段简报 | 10:00 早盘、11:35 午盘、14:45 尾盘 | 风险优先、市场概况、关键持仓/候选变化 |
| 服务异常 / 已恢复 | 自愈后仍失败或故障扩大；此前已成功通知的故障被实际验证恢复 | 对行情提醒/纪律提醒的影响，不展示技术字段 |

时间均为 Asia/Shanghai。简报使用两分钟启动窗，模型繁忙可在窗内启动；
缺少新鲜证据时不生成虚假报告。日历、数据新鲜度与覆盖率门槛不放宽。
2026-09-23 10:02 起，三次 Codex 简报的运行模型改为 `gpt-6-sol/high`
（当天 10:00 简报在思考强度切换前使用 `gpt-6-sol/medium`）；
10:00、11:35、14:45 时点不变。`QUANT_INTRADAY_ADVISORY_CODEX_SECONDS=1800`
是旧配置兼容项，当前 `notice-v2` 不按每 30 分钟发送 Codex 简报。
10:00 / 11:35 / 14:45 按日期和类别使用持久化投递键，重启也不重复排队；
未成功生成时可在窗内间隔至少 60 秒重试一次。发送失败沿用既有有界 outbox 重试。
启动窗不保证分钟整点立即送达，也不在窗外追补旧盘面。

09:25 / 09:32 自检继续后台运行，正常、跳过以及未通知过的自动恢复均静默。
故障通知成功后，状态存入 `G:/StockPlatform/logs/runtime/opening-guard-notification.json`，
同一故障去重、新增失败项才再次通知。只有此前故障检查项全部通过，才通知一次恢复；
盘前检查不能把尚未验证的开盘行情故障称作恢复。发送失败不推进已通知状态。
`--as-of` 历史/手工模拟不发送通知，也不推进通知状态。恢复检查仍是已有两个任务时点，
本次没有新增全天运维监控任务；不能宣称故障恢复后会即时主动通知。

## 去重与模型边界

- 移除普通异动/纪律事件之后的 45 秒 Codex 跟随调用；确定性证据不再跟一张模型复述卡。
- 十分钟轮询仍在，但仅价格、量能、多空类别变化不会再启动模型。没有已通知基线时，
  不编造“原计划发生变化”；先由阶段简报建立当日基线。
- 可触发条件更新的字段见 `notice_policy.CONDITION_LABELS`：纪律/候选条件、计划阶段、
  持仓数量/可卖数量等。价格变动引起的市值与仓位百分比不是条件更新。
- 这不是额外的模型择时链路：行情变化由确定性卡承担，纪律是否触发由纪律规则承担。
  模型不得更改原计划、创造价格线、用委比投票或把候选当持仓。
- 条件更新幂等键是日期 + 代码/条件状态/上次已通知回执指纹，换措辞或换模型不产生第二条；
  同日真实 A→B→A→B 条件往返仍可分别通知，不因曾到过 B 就永久压制。
- 发送个股下跌卡前，查询同账户、同股、相邻 90 秒的止损/移动/时间纪律事件。
  **只有对应纪律卡已经发成功**，才将尚未发送的普通下跌卡记为 disabled /
  covered_by_discipline。未发送成功的纪律卡不覆盖价格告警，避免吞掉用户尚未看见的风险。
- 不压制其他股票、其他账户、候选、上涨或止盈事件。不能撤回已经发出的普通卡，
  也不推迟后发生的紧急纪律卡来凑“一条消息”；两条独立链路存在这个明确的竞态边界。
- 个股与行业缺少已核验映射时，不硬拼为板块共振；本次没有新增跨股聚合规则。

## 展示约定

- 红色表达紧急风险，不用来表示上涨；普通关注橙色，恢复/条件更新/常规简报蓝色。
- 个股卡标题直接显示对象与变化；首屏保留明确时间区间、涨跌和量能。
- 3/5 分钟触发不能藏在平静的最后一分钟后面；完整 1/3/5 分钟证据折叠。
- 委比、内外盘分类差、单位成交额价格响应在证据层，缺失值不补零，不合成任意总分。
- 阶段简报风险不折叠，不再拿第一只持仓动作当整个账户指令。
- 无新增类别/计划变化的逐股条目默认不再展开；未列出不代表停止监控。
- 简单条件显示前后值；复杂结构指向工作台，不把内部 JSON 原样发给用户。
- 模型来源放页脚；按钮诚实标为“打开决策工作台”，不冒充已实现的个股深链。

## 盘后边界

已核对 `daily_strategy_summary_runtime.py`：19:15—19:30 日终摘要只写前端，
delivery_status 为 suppressed，不发送飞书。此次保留该策略，**没有新增收盘飞书卡**，
也不改协作者的教师卡片、新闻、模拟盘通知。此前把“已有日终摘要入口”理解成
“已有收盘飞书卡”是不准确的。

## 验收与回退

- 新纯函数、CLI 持久状态、运行时不跟发、简报重启幂等、展示层回归在
  `quant-service/tests/test_intraday_notice_policy.py` 及现有三个盘中测试模块。
- `scripts/verify-intraday-notice-policy.py` 使用生产只读连接：实际执行 repository 查询，
  使用 VALUES CTE 测试八个跨卡去重正反例；不创建临时表、不插入/删除业务数据。
  同时渲染历史异动卡和清楚标记的合成简报，输出到指定 G: 证据目录。
- 本次实现无数据库迁移、无环境变量修改、无券商操作。
- 回退用标准 `switch-stock-release.ps1` 切换到本次发布前的保留版本；不要编辑 current。
- 收盘后验收不能代替下一个交易日实流验收，也不能把飞书 API 接收成功说成手机已响铃。

源代码针对性测试、完整发布门禁和部署读回结果在本次交付回执中分别记录。

## 本次交付回执

- 主实现提交：`2724f8210fcc241fcebc8d889e4a0024e9543ffa`。
- 最终生产提交：`3d8b1f9b482c0d5823c0f085c1191da77dc0a387`。
  后一提交修正状态接口/配置检查工具遗留的半小时说明，新增 HTTP 合同回归。
- 最终 release：`20260922T165120-3d8b1f9b482c-clean`，
  2026-09-22 16:55:42+08:00 激活。未使用 AllowDirty / SkipTests。
- 针对性测试：52 passed；最终发布后端：3252 passed、130 skipped、893 subtests passed。
  跳过项不计入真实环境验收。前端 36 文件 / 144 tests passed，typecheck / build 通过；
  适配层 116 tests passed；Windows 生命周期门禁通过。
- 新版生产目录运行只读验收：8 个 PostgreSQL 去重正反例全部通过，36 张今日历史卡片
  渲染校验通过，另有 3 张明确标记的合成阶段简报预览。证据：
  `G:/StockPlatform/data/research/intraday-notice-v2-20260922/production-acceptance.json`。
- DeepSeek `deepseek-flash` 与 Codex `gpt-5.6-sol/medium` 的真实调用通过身份、条件、
  数字单位和卡片结构验证；输入为合成测试，不落库、不推送，不是当前投资建议。
- `/health` 和真实 adapter `/api/research/runtime/health` 均为 ok，并返回最终生产 SHA；
  状态接口 `cadence` 与配置检查工具均显示 `notice-v2`、三次简报及不跟发。
  mounted OpenAPI 与前端类型 196 条路径一致。
- 最终后台心跳读回 `2026-09-22T16:57:48.652115+08:00`，晚于新版本激活；
  `last_error=null`，收盘后 state=idle，notification_policy=notice-v2。
  已等待旧租约自然到期，没有手工删除租约或把旧状态当作新版本运行证据。
- 发布读回：本地 API、adapter、shared runtime 均 ok；remote owner / peer 均 200。
  两次发布均复用共享隧道，没有因本次卡片改动重启它。
- 本轮没有向飞书发送验收成功或测试卡；正常静默/故障去重/恢复语义通过隔离 CLI 测试验证。
  没有验证用户手机的实际显示、声音或震动，也没有冒充已经验收下一交易日的真实触发。
- 当前候选侧状态 `formal_recommendation_missing_or_inactive` 已单独核对：最新正式记录
  创建于 09-21 19:24，有效期至 09-22 15:00；这是收盘后过期，不是发布造成。
  收盘后 scope 为 4 只持仓，未擅自延长候选有效期。
- 可回退到中间版 `20260922T164209-2724f8210fcc-clean`；如需撤回整个 v2，
  使用仍保留的 `20260922T154232-fedabd5e10aa-clean`。
- 发布器按六版本保留策略清理旧包 `20260922T012644-9e917df1238b-clean` 和
  `20260922T020134-964b19d10abc-clean`；源码仍在 Git，可重建，不是业务数据删除。
  旧 `20260920T195304-a82e80b5eb0f-clean` 的占用文件清理被延后，没有强制终止进程。
